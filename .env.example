# YouTube Transcript Collector - Environment Configuration

# =============================================================================
# PROXY CONFIGURATION (Optional)
# =============================================================================
# Use proxies to avoid YouTube IP blocking/rate limiting
# Recommended: Webshare rotating residential proxies
# https://www.webshare.io/

# Enable/disable proxy usage
USE_PROXY=false

# =============================================================================
# WEBSHARE API KEY (For API-based rotation)
# =============================================================================
WEBSHARE_API_KEY=your-api-key-here

# =============================================================================
# PROXY MODE
# =============================================================================
# Choose proxy mode: 'rotating', 'api', or 'manual'
#
# - 'rotating' = Webshare rotating residential (RECOMMENDED)
#   IP changes automatically on each request - best anti-blocking
#
# - 'api' = Fetch proxy list from Webshare API, rotate manually
#   Good for moderate usage, caches 20 proxies for 60 minutes
#
# - 'manual' = Use single static proxy
#   Simple but less resilient to blocking

PROXY_MODE=rotating

# =============================================================================
# ROTATING RESIDENTIAL PROXY (Used if PROXY_MODE=rotating)
# =============================================================================
# Webshare rotating residential proxies
# Format: username-rotate for IP rotation on every request
# Options: username-rotate, username-rotate-US, username-rotate-GB, etc.

ROTATING_PROXY_HOST=p.webshare.io
ROTATING_PROXY_PORT=80
ROTATING_PROXY_USERNAME=your-username-rotate
ROTATING_PROXY_PASSWORD=your-password-here

# =============================================================================
# MANUAL PROXY (Used if PROXY_MODE=manual)
# =============================================================================
# Single static proxy configuration

PROXY_HOST=proxy.webshare.io
PROXY_PORT=80
PROXY_USERNAME=your-username-here
PROXY_PASSWORD=your-password-here

# =============================================================================
# USAGE INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Get your Webshare credentials:
#    - Sign up at https://www.webshare.io/
#    - For rotating proxies: Use "username-rotate" format (e.g., myuser-rotate)
#    - For API mode: Get API key from dashboard
#    - For manual: Get individual proxy credentials
#
# 3. Edit .env and add your credentials
#
# 4. Set USE_PROXY=true and choose PROXY_MODE
#
# 5. Run the app normally:
#    streamlit run app.py
#
# 6. The app will automatically retry up to 5 times if fetching fails

# =============================================================================
# NOTES
# =============================================================================
# - The .env file is in .gitignore (won't be committed to git)
# - Proxies are only used for transcript fetching, not YouTube Data API
# - Rotating residential proxies work best (datacenter IPs are often blocked)
# - Retry mechanism: 5 attempts with different IPs before giving up
# - Each retry uses a fresh IP when using rotating mode
